# Engineering rules
Never commit secrets, .env files, uploads, tokens, or private content. Use typed schemas and ownership-scoped queries. Do not add fake success paths. Add migrations for schema changes and tests for behavior. Keep changes focused; run format, lint, type, tests, and Docker Compose validation before delivery.
