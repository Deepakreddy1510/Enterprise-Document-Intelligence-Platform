"use client"; import {useEffect,useState} from "react"; import {api} from "../lib/api";
export default function Home(){const [state,setState]=useState("Checking API…");useEffect(()=>{api("/health").then(()=>setState("API is running. Sign in to manage your PDFs and conversations.")).catch(()=>setState("API is unavailable. Start Docker Compose and retry."))},[]);return <main className="shell"><h1>Enterprise RAG Assistant</h1><p className="muted">A focused, multi-document PDF question-answering application.</p><section className="card"><h2>Foundation status</h2><p>{state}</p><a href="/login">Log in</a> · <a href="/register">Create account</a></section></main>}
